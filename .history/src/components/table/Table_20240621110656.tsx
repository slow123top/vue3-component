import { defineComponent, ref, Teleport, Transition } from "vue";

export default defineComponent({
  name: "bea-table",
  props: {},
  setup(props, { emit }) {
    return ()=>(
        <div>
            
        </div>
    );
  },
});
